.side__bar
  .user__name
    .user__box
      %p.name__box
      = current_user.name
      %ul.name
        %li.name__icon
          = link_to "#" do
            = icon('fa', 'edit', class:'icon')
        %li.name__icon
          = link_to edit_user_path(current_user) do
            = icon('fas', 'cog', class:'icon')
  .group__list
    .group
      %p.group__name
        あ
      %p.group__message
        い